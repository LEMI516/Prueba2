<policies>
  <inbound>
    <base />
  </inbound>
  <backend>
    <base />
  </backend>
  <outbound>
    <base />
    <set-body>@{
      var src = context.Response.Body.As<JObject>(preserveContent: true);
      if (src == null)
      {
        return context.Response.Body.As<string>(preserveContent: true);
      }

      // Remover campos sensibles comunes si existen en el payload.
      src.Property("password")?.Remove();
      src.Property("token")?.Remove();
      src.Property("secret")?.Remove();
      src.Property("cvv")?.Remove();
      src.Property("pan")?.Remove();
      src.Property("cardNumber")?.Remove();
      src.Property("ssn")?.Remove();

      return src.ToString();
    }</set-body>
  </outbound>
  <on-error>
    <base />
  </on-error>
</policies>
